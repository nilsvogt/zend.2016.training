<h1>there we are</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url('blog/create'));
$form->prepare();

$form->get('submit')->setValue('CREATE');

echo $this->form()->openTag($form);

echo $this->formCollection($form);

echo $this->form()->closeTag();